spring:
  jpa:
    show-sql: false

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000}

logging:
  level:
    org.springframework.security: WARN
    com.devblo: INFO
    org.flywaydb: WARN
